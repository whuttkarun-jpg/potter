// หน้าแรก = แอปที่ย้ายมาจาก AI Studio (Vite+React) ทั้งชุด
// โค้ดต้นฉบับอยู่ในโฟลเดอร์ ais/ (ais/App.tsx + components + data)
import AisApp from "@/ais/App";

export default function Home() {
  return <AisApp />;
}
